---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CodeBlock from '../../components/CodeBlock.astro';
---

<BaseLayout title="快速上手 — sift 文档">
  <article class="doc container">
    <span class="eyebrow">Docs</span>
    <h1>快速上手</h1>
    <p>sift 以 npm 包 <code>@agent-context/sift</code> 提供 Node.js API,核心为纯 Rust(napi-rs 绑定)。安装时自动命中当前平台的二进制子包。适合"上下文里塞了大量机器产出"的场景——这些内容 token 多、冗余高,但真正对任务有用的信息密度低。</p>

    <h2>安装</h2>
    <CodeBlock code={`npm install @agent-context/sift`} lang="bash" command />

    <h2>直接接入 Pi / OpenCode</h2>
    <p>如果使用 Pi 或 OpenCode，无需自己编写 hook：现成适配器会自动压缩新产生的工具输出，并注册 <code>sift_retrieve</code>，让 Agent 在需要时取回 stash 原文。</p>

    <h3>Pi</h3>
    <CodeBlock code={`pi install npm:@agent-context/pi-sift`} lang="bash" command />

    <h3>OpenCode</h3>
    <p>在用户级或项目级 <code>opencode.json</code> 中加入：</p>
    <CodeBlock code={`{
  "$schema": "https://opencode.ai/config.json",
  "plugin": [
    ["@agent-context/opencode-sift", { "minLength": 200 }]
  ]
}`} lang="json" />
    <p>完整安装、配置、stash 存储与排障说明见 <a href="https://github.com/agents-sdk/sift-plugins">agents-sdk/sift-plugins</a>。</p>

    <h2>最小示例</h2>
    <CodeBlock code={`import { createSift, siftRequest, siftText, retrieve, retrieveLines } from "@agent-context/sift";

// 1. 压缩请求体:自动检测 Anthropic /v1/messages、
//    OpenAI Chat Completions、OpenAI Responses 三种格式
const { body, changed, tokensSaved } = siftRequest(requestBody, "用户当前的问题");

// 2. 或压缩源码;折叠点直接指向落盘 stash 文件
const r = siftText(fileContent, "error mismatched");
// // ... 30 lines omitted from file "/home/agent/.sift/stash/HASH", starting at line 32

if (r.lossy) {
  // 有损:原文已入 stash store,输出尾部带 «stash:HASH» 标记
  const original = retrieve(r.stashKey!);
  // 若输出提示给出省略行范围,也可以只取回所需分片
  const fragment = retrieveLines(r.stashKey!, 120, 80);
}`} lang="ts" />

    <h2>指定 stash 目录</h2>
    <p>直接导入的 <code>siftRequest</code>、<code>siftText</code>、<code>retrieve</code> 和 <code>retrieveLines</code> 使用共享的默认 store:优先读取 <code>SIFT_STASH_DIR</code>,其次是 <code>~/.sift/stash</code>,最后才回退到系统临时目录。如果应用需要隔离租户、任务或测试数据,用 <code>createSift</code> 创建绑定到指定目录的实例:</p>
    <CodeBlock code={`import { createSift } from "@agent-context/sift";

const sift = createSift({
  stashDir: "/var/lib/my-app/sift-stash",
});

const result = sift.siftText(toolOutput, "error mismatched");
const original = result.stashKey
  ? sift.retrieve(result.stashKey)
  : null;`} lang="ts" />
    <p><code>stashDir</code> 必须是非空字符串;相对路径按调用 <code>createSift</code> 时的工作目录解析。每次调用都会创建独立实例,所以同一 Node.js 进程可以同时使用多个 stash 目录。压缩和恢复要使用同一个实例,顶层 <code>retrieve</code> 不会读取实例的独立目录。</p>

    <h2>典型场景</h2>

    <h3>Agent 的工具输出</h3>
    <p>编码 Agent 每轮都会往上下文里塞构建日志、grep 结果、git diff。几轮之后,工具输出占了绝大部分 token,而且旧轮次的输出早就没用了。</p>
    <CodeBlock code={`for (const step of agentSteps) {
  const out = await runTool(step);
  // 送进模型前先压缩,query 用当前任务描述
  const r = siftText(out, step.intent);
  messages.push({ role: "tool", content: r.text });
  if (r.lossy) keepForRecovery(r.stashKey);
}`} lang="ts" />
    <p>要点:<code>query</code>(当前意图)帮助相关性筛选的压缩器保留关键行;纯文本也会把 query 纳入 BM25 相关性评分。</p>

    <h3>长对话的历史消息</h3>
    <p>对话越长,每轮的输入成本越高。在发送请求前对整个 body 跑一遍 <code>compress</code>,早期消息里的日志、JSON、diff 会被压缩,而最近的轮次与冻结前缀保持原样。</p>
    <CodeBlock code={`// 发送前一步,自动检测 Anthropic / OpenAI 格式
const { body, tokensSaved, frozenMessages } = siftRequest(requestBody, userQuestion);
await fetch("/v1/messages", { method: "POST", body: JSON.stringify(body) });`} lang="ts" />
    <p>要点:<code>cache_control</code> 以下的消息是缓存锚点,sift 一个字节都不动,不会破坏你的 prompt cache 命中。</p>

    <h3>把大文件喂给 LLM</h3>
    <p>分析线上问题、读监控数据时,原始文件往往几十 KB 起步。先 <code>siftText</code> 再送入:</p>
    <ul>
      <li><strong>监控 / 导出的 JSON 对象与数组</strong>:递归 schema 去重 + 采样,异常行优先保留,连续对象也会规范化处理;</li>
      <li><strong>构建 / 测试日志</strong>:错误与堆栈全保留,重复 warning 折叠成一条;</li>
      <li><strong>git diff</strong>:lockfile / 纯空白变化摘要,hunk 采样,改动行保留;</li>
      <li><strong>会议纪要等长文本</strong>:按相关性、位置和显著信息抽取代表句段,并抑制近重复内容;完整原文可从 stash 恢复。</li>
    </ul>

    <h2>API 一览</h2>
    <h3><code>createSift(&#123; stashDir &#125;)</code></h3>
    <p>创建绑定到独立 stash 目录的 API 实例。实例提供 <code>siftRequest</code>、<code>siftText</code>、<code>retrieve</code>、<code>retrieveLines</code>、<code>detectContentType</code> 和 <code>detectRequestFormat</code>;不影响直接导入的顶层 API 及其默认目录逻辑。</p>

    <h3><code>siftRequest(body, query?)</code></h3>
    <p>压缩请求 body(就地透传或压缩),返回:</p>
    <table>
      <tr><th>字段</th><th>说明</th></tr>
      <tr><td><code>body</code></td><td>压缩后的请求 body(格式与输入一致)</td></tr>
      <tr><td><code>changed</code></td><td>是否发生了实际压缩</td></tr>
      <tr><td><code>blocksExamined / blocksCompressed / blocksReverted</code></td><td>检查 / 压缩 / 因 token 校验回退的 text block 数</td></tr>
      <tr><td><code>frozenMessages</code></td><td>冻结前缀消息条数(cache 锚点,未被触碰;OpenAI 格式恒为 0)</td></tr>
      <tr><td><code>stashStored</code></td><td>写入 stash store 的原文条数</td></tr>
      <tr><td><code>tokensSaved</code></td><td>估算节省的 token 数</td></tr>
    </table>

    <h3><code>siftText(text, query?, sourcePath?)</code></h3>
    <p>压缩单个字符串(把工具输出原文送进任意 API 之前),返回 <code>text / changed / lossy / stashKey / tokensSaved</code>。使用落盘 stash 时,源码、搜索结果、日志以及显式保守模式的整行纯文本可在省略位置输出 <code>[... 30 lines omitted from file "/home/agent/.sift/stash/HASH", starting at line 32]</code>,代码使用注释形式。文件是完整原文,起始行从 1 计数,不是搜索命中源码的行号。默认纯文本按句子抽取,不伪造行范围,通过末尾 stash 标记恢复。Git Diff 在逐段绝对路径会抵消收益时改用紧凑文件/hunk 汇总。可选的 <code>sourcePath</code> 仅用于按扩展名选择 8 种源码 grammar。单 block 最小 512 字节才参与压缩。</p>
    <p>无损 minify 不产生 stash 或省略提示。JSON 结构采样、行内片段、标签保护造成映射变化、非本地 store 等情况暂不输出文件行提示。纯文本默认按 query 相关性、位置、显著信息和近重复抑制进行抽取,保留的句段逐字来自原文。</p>
    <p>日志首个非空行、可识别的命令回显(如 <code>$ cargo build</code>、shell tracing、npm script、PowerShell 提示符)及显式续行强制保留,不会被普通日志行数预算截掉或隐藏在模板中。能从 stash 恢复不等于允许省略执行命令。</p>

    <h3><code>retrieve(key)</code></h3>
    <p>按 <code>stashKey</code> 从 stash store 取回有损压缩的原文,端到端无损。</p>

    <h3><code>retrieveLines(key, startLine, lineCount)</code></h3>
    <p>按 stash 原文的 1-based 行号读取连续分片,单次最多 1000 行;返回 <code>text / startLine / lineCount / totalLines / hasMore</code>。这是非本地 stash 或文件工具不可用时的备选;代码内联提示本身不要求调用这个 API。</p>

    <h3><code>detectContentType(text)</code> / <code>detectRequestFormat(body)</code></h3>
    <p>类型探测:返回内容类型(JSON 对象或数组,也识别连续对象与 JSON 为主体的轻量 wrapper / 构建日志 / 搜索结果 / git diff / 源代码 / 纯文本 / HTML)或请求格式(anthropic / chat_completions / responses)。</p>

    <h2>最佳实践</h2>
    <ul>
      <li><strong>按需传 query</strong>:用户当前的问题、Agent 当前步骤的意图可帮助相关性筛选,纯文本默认也会使用 query。</li>
      <li><strong>按原文坐标读取</strong>:模型只追问局部时,优先用 Agent 已有的文件分片读取能力;原文不可直接访问时,再用 <code>retrieveLines</code> 从 stash 读取。需要全文时可用 <code>retrieve</code>。</li>
      <li><strong>成对使用实例 API</strong>:通过 <code>createSift</code> 压缩后,必须通过同一实例的 <code>retrieve</code> 恢复;顶层 API 和其他实例可能指向不同目录。</li>
      <li><strong>不用担心理解偏差</strong>:压缩只发生在消息内部,对话结构、tool_use/tool_result 配对完整保留;API key 等高熵凭证被强制识别并保留。</li>
      <li><strong>短内容不用管</strong>:单 block 小于 512 字节不参与压缩,直接透传,零开销。</li>
    </ul>

    <div class="doc-nav">
      <a href="/docs/how-it-works"><b>工作原理</b>压缩管线 · 三大不变量 · 压缩器类型</a>
      <a href="/#demo"><b>返回首页演示</b>粘贴自己的内容试试</a>
    </div>
  </article>
</BaseLayout>
