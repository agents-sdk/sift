---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="使用场景与最佳实践 — compressor 文档">
  <article class="doc container">
    <h1>使用场景与最佳实践</h1>
    <p>compressor 适合"上下文里塞了大量机器产出"的场景——这些内容 token 多、冗余高,但真正对任务有用的信息密度低。下面是几个典型用法。</p>

    <h2>场景一:Agent 的工具输出</h2>
    <p>编码 Agent 每轮都会往上下文里塞构建日志、grep 结果、git diff。几轮之后,工具输出占了绝大部分 token,而且旧轮次的输出早就没用了。</p>
    <pre><code is:raw>for (const step of agentSteps) {
  const out = await runTool(step);
  // 送进模型前先压缩,query 用当前任务描述
  const r = compressText(out, step.intent);
  messages.push({ role: "tool", content: r.text });
  if (r.lossy) keepForRecovery(r.ccrKey);
}</code></pre>
    <p>要点:每步都传 <code>query</code>(当前意图),压缩器会保留与任务相关的行(错误、匹配项、改动),丢弃重复样板。</p>

    <h2>场景二:长对话的历史消息</h2>
    <p>对话越长,每轮的输入成本越高。在发送请求前对整个 body 跑一遍 <code>compress</code>,早期消息里的日志、JSON、diff 会被压缩,而最近的轮次与冻结前缀保持原样。</p>
    <pre><code is:raw>// 发送前一步,自动检测 Anthropic / OpenAI 格式
const { body, tokensSaved, frozenMessages } = compress(requestBody, userQuestion);
await fetch("/v1/messages", { method: "POST", body: JSON.stringify(body) });</code></pre>
    <p>要点:<code>cache_control</code> 以下的消息是缓存锚点,compressor 一个字节都不动,不会破坏你的 prompt cache 命中。</p>

    <h2>场景三:把大文件喂给 LLM</h2>
    <p>分析线上问题、读监控数据时,原始文件往往几十 KB 起步。先 <code>compressText</code> 再送入:</p>
    <ul>
      <li><strong>监控 / 导出的 JSON 数组</strong>:schema 去重 + 采样,异常行优先保留;</li>
      <li><strong>构建 / 测试日志</strong>:错误与堆栈全保留,重复 warning 折叠成一条;</li>
      <li><strong>git diff</strong>:hunk 采样,改动行保留;</li>
      <li><strong>会议纪要等长文本</strong>:BM25 抽取关键句,支持中文。</li>
    </ul>

    <h2>最佳实践</h2>
    <ul>
      <li><strong>总是传 query</strong>:用户当前的问题、Agent 当前步骤的意图。相关性打分依赖它,不传则保留策略更"盲"。</li>
      <li><strong>保留 ccrKey</strong>:有损压缩的原文在 CCR store 里,把 key 记下来,模型追问细节时用 <code>retrieve(key)</code> 取回原文再注入,端到端无损。</li>
      <li><strong>不用担心理解偏差</strong>:压缩只发生在消息内部,对话结构、tool_use/tool_result 配对完整保留;API key 等高熵凭证被强制识别并保留。</li>
      <li><strong>短内容不用管</strong>:单 block 小于 512 字节不参与压缩,直接透传,零开销。</li>
    </ul>

    <div class="doc-nav">
      <a href="/docs/architecture">← 三大不变量与架构</a>
      <a href="/">→ 返回首页,试试粘贴自己的内容</a>
    </div>
  </article>
</BaseLayout>
